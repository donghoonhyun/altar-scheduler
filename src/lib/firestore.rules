rules_version = '2';
service cloud.firestore {
  match /databases/{db}/documents {
    
    // 로그인만 되어 있으면 모든 문서 읽기/쓰기 허용
    match /{document=**} {
      allow read, write: if request.auth != null;
    }

  }
}
