rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{path=**} {
      allow read, write: if true; // 테스트용 전체 허용
    }

    match /{document=**} {
      // allow read, write: if true;
      allow read, write: if request.auth != null;
    }

    // ✅ users, members 등 나머지
  }
}
